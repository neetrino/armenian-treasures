/**
 * Public inner-page hero frame — keep in sync with `.khndzoresk-page .hero` in khndzoresk.css.
 */
export const PAGE_HERO_MIN_HEIGHT_CLASS =
  'min-h-[clamp(520px,82svh,760px)] max-md:min-h-[72svh]';

/**
 * Compact admin preview ratio (~2.4:1) approximates public full-width hero crop on desktop.
 */
export const PAGE_HERO_ADMIN_PREVIEW_ASPECT_CLASS = 'aspect-[12/5]';

/** Home hero admin preview — shorter than live 100svh hero. */
export const HOME_HERO_ADMIN_PREVIEW_ASPECT_CLASS = 'aspect-[16/9]';
